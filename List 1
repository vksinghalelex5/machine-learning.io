fruits = ["apple", "banana", "cherry"]
for x in fruits:
  print(x) 

fruits = ["apple", "banana", "cherry"]

# Initialize the index variable
i = 0

# Loop until the index reaches the length of the list
while i < len(fruits):
    print(fruits[i])
    i += 1  # Increment the index to move to the next item

output: 

apple
banana
cherry
